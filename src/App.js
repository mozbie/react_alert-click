import React from 'react';
import './App.css';

function App() {
  return (
    <button type="button" onClick={() => alert('Button was clicked!')}>
      Button
    </button>
  );
}

export default App;
